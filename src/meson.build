subdir('editor')
subdir('platform')
subdir('sdl')
subdir('widgets')

source_files = files(
    'config.cc',
    'logger.cc',
    'main.cc',
    'utils.cc',
) + editor_source_files + platform_source_files + sdl_source_files + widgets_source_files